package com.chainsys.jdbc;

import java.io.BufferedReader;
import java.io.DataInputStream;
import java.io.InputStreamReader;
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.Statement;

public class UpdateDataBase {

	public static void main(String[] args) 
	{
		try
		{
		
		//DataInputStream dr=new DataInputStream (System.in);
			BufferedReader dr=new BufferedReader (new InputStreamReader (System.in));
		
		  System.out.println("Enter Jersy no");
	        int jersy_no =Integer.parseInt(dr.readLine());
	       System.out.println("Enter Player Name");
	        String players_name =dr.readLine();
	        System.out.println("Enter Score");
	        int score =Integer.parseInt(dr.readLine());
	        System.out.println("Enter Salary");
	        int salary =Integer.parseInt(dr.readLine());
	        System.out.println("Enter Team ID");
	        int team_id =Integer.parseInt(dr.readLine());
	        System.out.println("Enter AGE");
	      int age =Integer.parseInt(dr.readLine());
	      Connection con;
	      Statement st;
	      con=DriverManager.getConnection("jdbc:oracle:thin:@localhost:1521:xe","system","oracle");
	      st= con.createStatement();
	      String sqlupdate ("update ")
		}

	}

}
